{
  "version": "0.0.1-alpha.3",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^3.9.5",
    "@types/jest": "^26.0.0",
    "@types/supertest": "^2.0.9",
    "jest": "^26.0.1",
    "supertest": "^4.0.2",
    "ts-jest": "^26.1.1",
    "ts-node": "^8.10.2",
    "np": "^6.2.4"
  },
  "scripts": {
    "build": "yarn run build:core",
    "build:core": "yarn workspace @restyjs/core run build",
    "release": "",
    "release:core": "yarn workspace @restyjs/core run",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testPathIgnorePatterns": [
      "/node_modules/",
      "/dist/"
    ]
  }
}